# Gemini powers the three agents (recovery, risk, reconcile).
# Real AI Studio keys start with "AIza" and are 39 chars.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Where the ledger and generated data live.
CHECKPOST_DB=data/checkpost.db

# Razorpay test mode only. Never a live key.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
